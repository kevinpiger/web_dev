# AGENTS.md

This file provides guidance to Codex (Codex.ai/code) when working with code in this repository.

## 現況

本目錄目前**沒有任何程式碼**，只有 `AGENTS.md` 與 `memory.md`，且尚未 `git init`。
下次有人在此新增實際程式碼後，應重跑 `/init` 補上下列各節。

## 專案慣例

- `memory.md`：專案操作記錄，以表格形式記錄（日期 / 操作類型 / 說明 / 備註）。有值得留存的操作時附加一列，不要重寫既有內容。
- `.Codex/skills/`：放置本專案可攜的執行規則（目前為空）。

## 待補（有程式碼後填寫）

- 技術棧與執行環境
- 建置 / 啟動 / 測試指令（含執行單一測試的方式）
- 跨檔案才看得出來的架構脈絡
